import React from 'react'
import { connect } from 'react-redux'

export const Kelas = (props) => {
    return (
        <div>Kelas</div>
    )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Kelas)